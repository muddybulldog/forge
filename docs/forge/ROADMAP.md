# Roadmap

## Upgrade cycle — 2026-07 ([ideation doc](ideas/2026-07-02-upgrade-cycle.md))

- [done] Phase 1: Pipeline & document-contract skill edits — three-gear routing, living specs, ideation handoff, brainstorm flow edits, verbosity contract, TDD trim, Global Constraints plan header ([spec](specs/2026-07-02-phase1-pipeline-skill-edits-design.md), [plan](plans/2026-07-02-phase1-pipeline-skill-edits.md))
- [done] Phase 2: Execution efficiency & scripts — file-referenced briefs, thin orchestrator, reviewer integrity, rework guardrails, tier-down preference; task-brief extractor + review-packet generator scripts ([spec](specs/2026-07-02-phase2-execution-efficiency-design.md), [plan](plans/2026-07-02-phase2-execution-efficiency.md))
- [done] Phase 3: Codex dual-harness packaging — SKILL.md port, TOML tier agents, SessionStart hook, harness-conditional execution ([spec](specs/2026-07-03-phase3-codex-dual-harness-design.md), [plan](plans/2026-07-03-phase3-codex-dual-harness.md))
- [done] Phase 4: Codex exec runner — deterministic whole-plan task runner over `codex exec`, GPT-5.6 tier mapping, receipts, mechanical rework cap; retires in-session dispatch and TOML agents ([spec](specs/2026-07-13-codex-exec-runner-design.md))
- [done] Phase 5 (Codex runner): Runner commit discipline — commit each passed task (vertical slices), hard-fail on a dirty tree at run start, per-task review base becomes the prior commit (retires the stash snapshot); planning-skill authoring rule so env-gated skips can't pass as green. Fixes the final-review over-scoping bug (dirty tree swept whole cross-phase diff into every review). ([spec](specs/2026-07-13-codex-exec-runner-design.md), [plan](plans/2026-07-14-phase5-commit-discipline.md))
- [done] Phase 6 (Codex runner): Runner session awareness — `--notify` modal on every terminal event (escalation, contract error, completion), `--status --run-dir`, incremental `run.json`, Codex-only `UserPromptSubmit` hook injecting live run state; runner never backgrounded blind. Codex-only (Claude handles this natively); scope widening split to Phase 7; stall watchdog dropped (Claude-path concern). ([spec](specs/2026-07-13-codex-exec-runner-design.md) §Session awareness, [plan](plans/2026-07-14-phase6-session-awareness.md))
- [done] Tier policy recalibration (cross-harness) — reframes classification from "predict the tier" to "standard is the floor; move off it only on named evidence." Standard is the default; up to complex requires a named design decision (persisted in the `Tier:` line, disclosed in the offer), down to trivial requires demonstrated mechanicalness; file count/category/"feels complex" are not evidence. Effort defaults drop to each provider's recommended start (standard sonnet·medium/terra·medium; complex opus·high/sol·medium; trivial luna·low); the removed strong settings stay as the human's on-halt bump target. Reviewer = task tier + fresh context (not a stronger model): `REVIEW_MAP` retired, strength-escalation removed, final review at the plan's highest tier. Its own spec (not the Codex-runner spec, per the "no mixed implementations" constraint); **precedes Phase 7**, whose escalation machinery is the automatic form of its floor+evidence up-tier path. ([spec](specs/2026-07-16-tier-policy-recalibration-design.md))
- [done] Phase 7 (Codex runner): Runner scope autonomy — a fix/defer/halt **disposition matrix** (provenance in-diff/pre-existing × contract impact breaking/improvement, both runner-verified) replaces 4b's binary mechanical/decision; **convergence-based** rework (reviewer labels resolved/carried/new, runner halts on regression/stuck, backstop raised 2→5) replaces the raw count cap; final review gains the same rework loop; `--autofix auto|gate` (auto = matrix, default) chosen at the execution offer; a **terminal doc-sync stage** reconciles docs after all-green. Consumes the tier-policy floor as the escalation *target*, but does **not** auto-escalate model/effort — plain progress-based rework (the effort bump stays a human on-halt action). Resolves the "revisit the rework cap" note: converging work now runs past the old cap of 2; only genuine churn or a scope decision halts. This is the **Codex-more-autonomous** half of the harness convergence (DEFERRALS 2026-07-16). ([spec](specs/2026-07-16-phase7-scope-autonomy-design.md), [plan](plans/2026-07-16-phase7-scope-autonomy.md))
- [done] Phase 9 (Codex runner): Live run monitor — attach-from-outside `rich` TUI (`scripts/forge-monitor.py`) showing the plan ledger + the in-flight task's `codex exec` stream, with a terminal-state banner (halt/complete). Runner tees each subprocess to `task-N-live.log` and adds a live progress pointer + timestamps + heartbeat to `run.json` (which also resolves the stale-`running` liveness deferral). Reverses the 2026-07-15 monitor-terminal rejection — foreground turned out to be a blind blocking loop. Codex-only. ([spec](specs/2026-07-15-forge-run-monitor-design.md))
- [deferred] Phase 8 (Claude path): Execution parity — **decomposed 2026-07-17 into Phases 10–12** after a parity audit (Codex as the reference implementation of the post-Phase-7 target state) reframed the gap. The audit found the inline/dispatch choice is one shared **task-shape** criterion (context-is-an-asset / simple change) wrongly entangled with harness capability: Codex has **no inline path** — the runner (built for the multi-worker/scale case) became Codex's *only* execution path, so simple serial work that should run inline has nowhere lightweight to go. Since inline should exist symmetrically on both harnesses carrying the same canon, the original Half A (dispatch commit/clean-tree) + Half B (inline human-gate) are resequenced across 10–12 (below). Also surfaced the tier-recalibration drift (agent frontmatter still pins the old xhigh/high effort → Phase 12). See [DECISIONS 2026-07-17](DECISIONS.md).
- [done] Phase 10 (Codex path): Codex inline execution branch — give Codex the inline **vehicle** it lacks. The shared context-is-an-asset / simple-change criterion selects mode **before** harness (retiring the current "no Workflow tool → runner-only" routing); inline = the Codex session does the work itself (TDD, review-before-commit, commit-per-task), the runner reserved for the dispatch/parallel-scale case it was built for. Finding-handling **deliberately mirrors Claude's current (over-resolving) inline model** — this phase establishes the structural path and reaches symmetry with Claude; it does **not** fix the process (Phase 11 does). ([spec](specs/2026-07-17-phase10-codex-inline-design.md))
- [done] Phase 11 (cross-harness): Fix the inline finding-process — with both inline paths symmetric after Phase 10, correct the finding-handling once in the **shared home**: replace inline over-resolution with the Phase 7 disposition canon (fix/defer/halt + human gate) so a decision-grade finding surfaces at the gate instead of being silently auto-fixed. **Inline stays self-reviewed** — no separate fresh-context reviewer (that's a dispatch-only concern; TDD + acceptance commands carry the objective load, per [DECISIONS 2026-07-17](DECISIONS.md)); the fix **gates the self-review's finding-handling**, it does not add a reviewer. Shared authoring fixes Codex and Claude inline together. This is DEFERRALS 2026-07-16's **Claude-more-bounded** Half B, generalized to both harnesses. ([spec](specs/2026-07-17-phase11-inline-finding-process-design.md), [plan](plans/2026-07-17-phase11-inline-finding-process.md))
- Phase 12 (Claude path): Final Claude parity — split at kickoff (2026-07-17) into 12a (mechanical, done) and 12b (the Phase 7 canon, planned), bringing the Claude dispatch path into parity with the Codex runner. See [DECISIONS 2026-07-17](DECISIONS.md) and [DEFERRALS 2026-07-17](DEFERRALS.md).
  - [done] Phase 12a (Claude path): mechanical parity — tier-recalibration **effort drift** (agent frontmatter `forge-deep` xhigh→high, `forge-standard` high→medium — frontmatter is the only effort pin on the Claude path); retire the stale **escalation-reviewer** prose and condition the **final-integration-reviewer** role on "plan's highest tier is complex" (agent files can no longer imply always-deep; the skill already ties final review to the highest task tier); **Half A** — clean-tree precondition + commit-per-task on the **dispatch** branch (closing the final-review over-scoping gap Phase 5 fixed for Codex, and laying the per-task review base 12b's provenance check needs). ([DECISIONS 2026-07-17](DECISIONS.md))
  - [planned] Phase 12b (Claude path): the **Phase 7 canon on the Claude dispatch path** — the disposition matrix (fix/defer/halt) + convergence-based rework, today present only inline (Phase 11) and on the Codex runner (Phase 7), still missing on Claude dispatch (old cap-2 + raw finding→rework). Landing approach (decided 2026-07-17): **extract forge-run.py's disposition/convergence into a shared, tested `scripts/` helper with a CLI** (option C — Workflow JS can't shell out or import modules), driven by the **orchestrator** on the Claude path (Workflow fans out implementers; the orchestrator runs the shared helper for the deterministic loop) and by the runner on Codex — one tested implementation, two callers, preserving runner-verified provenance. Also: `--autofix auto|gate` equivalent at the Claude execution offer, final-review rework loop, terminal doc-sync, deferral write-back. Gets its **own spec** per the no-mixed-implementations constraint.
